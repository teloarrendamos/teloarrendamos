<h4>Listings</h4>

<ul>
  <% @listings.each do |listing| %>
    <li><%= link_to listing.description, listing %></li>
  <% end %>
</ul>

<%= link_to "New Listing", new_company_listing_path(current_company) %>