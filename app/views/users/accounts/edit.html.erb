<section class="accounts-edit">
  <div class="wrapper">
    <div class="info-column">
      <div class="account-info">
        <%= image_tag(@user.profile_picture) %>
        <div class="info-text">
          <h3><%= @user.first_name %> <%= @user.last_name %></h3>
          <p>+56 9 95773432</p>
          <p><%= @user.email %></p>
          <p>Miembro desde el 2016</p>    
        </div>
      </div>
      
      <button id="show-edit-form">Editar Perfil</button>  

      <div class="edit-account">
        <%= form_for @user, url: account_path do |f| %>
          <div class="row">
            <div class="form-group">
              Cambiar Foto de Perfil
              <%= f.file_field :profile_picture, class: "center-block" %>
            </div>

            <div class="form-group">
              <%= f.label :Nombre %>
              <%= f.text_field :first_name, class: "form-control" %>
            </div>

            <div class="form-group">
              <%= f.label :Apellido %>
              <%= f.text_field :last_name, class: "form-control" %>
            </div>

            <div class="form-group">
              <%= f.submit "Actualizar Perfil" %>
            </div>
          </div>

        <% end %>

        <%= link_to button_tag("Cambiar Contraseña", class: "btn btn-default"), edit_user_registration_path %>  
      </div>
    </div>

    <div class="map-column">
      <div class="map">
        <%= image_tag "map-image.png" %>
      </div>
    </div>  
  </div>

  <script>
    editButton = document.getElementById('show-edit-form');
    editButton.addEventListener('click', function() {
      document.getElementsByClassName('edit-account')[0].style.display = 'block';
    })
  </script>
</section>
